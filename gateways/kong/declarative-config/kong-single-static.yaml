_format_version: "3.0"

services:
  - name: example-service
    host: host.docker.internal
    port: 8888
    protocol: http
    routes:
      - name: example-route
        strip_path: false
        paths:
          - /api